import 'package:flutter/material.dart';

class MutualFundsView extends StatelessWidget {
  const MutualFundsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Mutual funds view'),),
    );
  }
}
