import 'package:flutter/material.dart';

class SpaceView extends StatelessWidget {
  const SpaceView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('SpaceView'),
      ),
    );
  }
}
